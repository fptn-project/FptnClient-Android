package org.fptn.vpn.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors res/values/color.xml so the Compose UI and the legacy XML views share colors.
val Primary = Color(0xFF1B023B) // @color/primary — deep purple, app / splash background
val Secondary = Color(0xFF03DAC5) // @color/secondary — teal accent
val White = Color(0xFFFFFFFF) // @color/white
val Black = Color(0xFF000000) // @color/black
val Hint = Color(0xFF999E9D) // @color/hint
val Yellow = Color(0xFFBCBC1C) // @color/yellow
val Gray = Color(0xFF959292) // @color/gray
val TealAccent = Color(0xFF03DAC5) // @color/teal_200
